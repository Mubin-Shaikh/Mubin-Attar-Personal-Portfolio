import blockContent from './blockContent'
import project from './project' // Import the new project schema
import experience from './experience' // Import the new experience schema

export const schemaTypes = [project, experience, blockContent]