# -------------------------------------------------------------
# GENERATED ARTIFACTS / DEPENDENCIES / BUILD OUTPUT
# -------------------------------------------------------------
# Node.js / npm / Vite
node_modules/           # All installed Node.js packages
dist/                   # Vite's production build output directory
.vite/                  # Vite's development server cache
package-lock.json       # Keep this file to ensure consistent installs across environments
# yarn.lock             # Uncomment if you are using Yarn and want to commit your lockfile
# pnpm-lock.yaml        # Uncomment if you are using pnpm and want to commit your lockfile

# Python
__pycache__/            # Python compiled bytecode cache
*.py[cod]               # Compiled Python files
*.pyo
*.pyd
.venv/                  # Generic virtual environment directory (used by pipenv, poetry, venv)
venv/                   # Another common virtual environment directory name
env/                    # Another common virtual environment directory name
ENV/                    # Another common virtual environment directory name
# *Note: Your project currently doesn't use a Python backend, but keep these if you add one later.*

# Sanity.io Studio (local development artifacts for the CMS itself)
.sanity/                # Sanity local cache and temporary files
.env.development        # Sanity Studio might create local .env files
.env.production
.env.local

# -------------------------------------------------------------
# SENSITIVE FILES / ENVIRONMENT VARIABLES
# -------------------------------------------------------------
.env                    # Generic .env file for environment variables (sensitive data)
.env.local              # Local environment variables specific to your machine
.env.*.local            # Other local environment variable files

# -------------------------------------------------------------
# LOGS / TEMP FILES / CACHE
# -------------------------------------------------------------
*.log                   # All log files
npm-debug.log*          # npm debug logs
yarn-debug.log*         # Yarn debug logs
yarn-error.log*         # Yarn error logs
*.pid                   # Process ID files
.cache/                 # Generic cache directory (e.g., from tools like browserslist)
coverage/               # Test coverage reports (if you add testing)

# -------------------------------------------------------------
# OPERATING SYSTEM / IDE SPECIFIC FILES
# -------------------------------------------------------------
.DS_Store               # macOS specific files
Thumbs.db               # Windows specific files
ehthumbs.db             # Windows specific files
Desktop.ini             # Windows specific files
.vscode/                # VS Code editor specific settings (often includes user preferences)
.idea/                  # JetBrains IDEs (PyCharm, WebStorm, etc.)
*.sublime-project       # Sublime Text project files
*.sublime-workspace     # Sublime Text workspace files
# .settings/            # Eclipse IDE

# -------------------------------------------------------------
# BACKUP / TEMPORARY FILES (Generic)
# -------------------------------------------------------------
*.bak                   # Backup files
*.old                   # Old versions of files
*.tmp                   # Temporary files
*.swp                   # Swap files (from editors like Vim)
*.seed                  # Seed files
*.orig                  # Original files from merges/patches
*.gz                    # Gzip archives (if not intended for distribution)
*.tgz                   # Tar Gzip archives (if not intended for distribution)